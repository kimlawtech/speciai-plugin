# SpeciAI 자문팀 플러그인 — 전기능 테스트 가이드

설치부터 스킬 10종·에이전트 6종·MCP 도구 9종·로컬 스크립트 4종·콘솔 8페이지·보안 게이트까지, 기능 하나마다 "이렇게 치면 → 이 스킬·도구가 발동하고 → 이 결과가 나와야 한다"를 적었다. 순서대로 돌리면 약 2시간, 핵심만(★ 표시) 40분.

Claude Code에서 발동 체인은 화면에 그대로 보인다 — `speciai_brief(domain: "skill_contract")` 같은 도구 호출 줄이 출력되므로 "기대 발동 체인"과 대조하면 된다.

---

## 0. 준비물

| 항목 | macOS | Windows |
|---|---|---|
| Node.js 18+ (`node -v`) | 공식 설치본 | 공식 설치본 — 한글 CSV 디코딩용 ICU 포함 |
| Claude Code (`claude --version`) | | + Git for Windows (Claude Code 선행 요구) |
| speciai.team 계정 | 구글 로그인 가능 | 동일 |
| 테스트 파일 | 은행 CSV 1개, 영수증 사진 1장, 기존 계약서 .docx 또는 .hwp 1개 (없으면 §6·§9 샘플 사용) | 동일 |

플러그인은 npm 의존성 0 — `npm install` 없음. 설치 용량 48MB(그중 33MB는 콘솔의 한글 HWP 편집기).

표기: `>` 는 Claude Code 프롬프트 입력, `$` 는 터미널 명령.

---

## 1. 설치 ★

```
$ claude plugin marketplace add kimlawtech/speciai-plugin
$ claude plugin install speciai-advisory@speciai
$ claude plugin list
```

| 확인 | 합격 기준 |
|---|---|
| plugin list | `speciai-advisory` 표시 |
| 설치 폴더 | macOS `~/.claude/plugins/marketplaces/speciai/speciai-advisory` / Windows `%USERPROFILE%\.claude\plugins\marketplaces\speciai\speciai-advisory` 아래 `skills/` 10개 폴더, `agents/` 6개, `mcp/server.mjs`, `scripts/` 6개 |
| Claude Code 재시작 후 `/mcp` | `speciai` 서버 connected, 도구 9개 |
| `/speciai-` 입력 후 자동완성 | advisory · bizplan · certmail · contract · criminal · jangbu · legaldoc · privacy · receipt · status 10개 |

---

## 2. 키 연결 ★

### 2-A. 콘솔 로그인(자동 발급) — 권장

```
$ node <설치경로>/scripts/dashboard.mjs
```
→ 브라우저 http://localhost:4747

| 단계 | 합격 기준 |
|---|---|
| 로그인 게이트 | 웹과 동일한 모달(이메일·구글) |
| 구글 로그인 | 동의 → 404 없이 콘솔 복귀 → 세션 유지 |
| 이메일 가입 | 확인 메일이 `SpeciAI <contact@speciai.ai.kr>`에서 5분 내 도착, 링크 클릭 시 콘솔로 복귀 |
| 유형 선택 | 기업 / 소상공인·사장님 / 전문직 3카드. 소상공인·전문직은 축약 온보딩(1페이지) |
| 온보딩 1단계 | 사업자등록번호 → "국세청 조회" → 초록색 `✓ 계속사업자 · 부가가치세 일반과세자` |
| 온보딩 완료 | "내 팀" 화면 진입, 새로고침해도 유지(운영 저장 확인) |
| 키 자동 발급 | 설정(톱니) 페이지에 `sk_spc_…` 마스킹 표시, `~/.speciai/config.json` 생성 |
| 사업정보 미러 | `~/.speciai/company.json`에 방금 입력한 회사명 |

### 2-B. 수동
speciai.team → 설정 → CLI 키 → 발급(1회만 표시) → `export SPECIAI_API_KEY=sk_spc_…`(Windows `setx`) → Claude Code 재시작.

### 2-C. 키 없음(음성 테스트)
키를 비운 상태에서:
```
> /speciai-advisory:status
```
합격: "인증 실패 … dashboard.mjs 실행 → localhost:4747 로그인하면 자동 발급" 안내가 뜨고 파이프라인은 진행되지 않는다.

---

## 3. 현황·기본 도구 ★

### 3-1. status 스킬
```
> /speciai-advisory:status
> 내 사용량 얼마나 썼지?
```
기대 체인: `status` 스킬 → `speciai_brief(skill_status)` → `speciai_status`
합격: 고정폭 박스(플랜·활성 팩·구독·크레딧·도구별 진행 바). 무료 한도 search 10 / law 20 / verify 20 / brief 30 / chat 5. 한도 70% 넘은 도구가 있으면 업그레이드 한 줄만(그 외 판촉 없음).

### 3-2. 국세청 사업자 조회 (speciai_biz_status)
```
> 사업자번호 192-34-01701 이 거래처 정상 사업자야?
> 이 세 개 거래처 휴폐업 확인해줘: 192-34-01701, 000-00-00000, 123-45-67890
```
합격: 첫 번호 `계속사업자 · 부가가치세 일반과세자`, 미등록 번호는 `미등록 … 매입세액 불공제 위험` 경고. 일괄 조회 최대 10건.

### 3-3. 인용 검증 (speciai_verify_citation)
```
> 근로기준법 제17조, 민법 제999조, 근로기준법 제999조 실존하는 조문인지 검증해줘
```
합격: 앞 둘 `✔ 실존 확인`, `근로기준법 제999조`는 `환각 의심` 판정. 판례 사건번호(`2023다12345` 형식)도 넣으면 법제처에서 조회.

### 3-4. 법제처 실시간 검색 (speciai_law_search)
```
> 근로기준법 최신 개정 공포일 확인해줘
> "임금체불 지연이자" 판례 찾아줘 (target prec)
```
합격: 법령명·공포일자·MST 번호 표시. 판례는 사건번호 포함.

### 3-5. RAG 검색 (speciai_search)
```
> 주휴수당 지급 요건 관련 법령·해설 검색해줘
```
합격: 유사도 점수 붙은 결과(0.55 미만은 근거로 안 씀 안내), "남은 오늘 검색 N회".

### 3-6. 팀 설정 조회 (speciai_team)
```
> 내 팀 설정 보여줘
```
합격: 콘솔에서 켠 에이전트 목록·맞춤 지시·저장된 워크플로우·사업 정보 요약(회사명·업종·규모).

---

## 4. 자문 오케스트레이션 ★

### 4-1. 단일 분야 — 자동 개입(슬래시 없이)
```
> 카페 알바 주 12시간 뽑으려는데 주휴수당 줘야 해?
```
기대 체인: `advisory` 스킬 자동 발동 → `speciai_brief(skill_advisory)` → `speciai_team` → `labor` 에이전트 스폰 → `speciai_brief(labor)` → `speciai_search`/`speciai_law_search` → `speciai_verify_citation` → 종합
합격: "노무 에이전트 투입" 내레이션 → 주 15시간 미만이라 미발생 결론 → [종합 결론(액션)] [분야별 요지] [기한] [근거] 구조, 근거는 조문 번호.

### 4-2. 복수 분야 병렬 + 상충 표시
```
> 직원 한 명 권고사직 시키려는데 위로금 주면 세금 어떻게 되고 노무상 절차는 뭐야? 5인 사업장, 입사 8개월
```
합격: labor + tax 에이전트가 병렬로 뜨고, 기한(해고예고·원천징수 신고), 분야 간 걸치는 부분은 [상충] 또는 [연계]로 표시.

### 4-3. 명시 호출
```
> /speciai-advisory:advisory 투자 받으면 세금이랑 계약서 어떻게 준비해야 해? 시드 3억 SAFE
```
합격: legal + tax + finance 투입, SAFE 특성(전환 전 지분 미확정) 반영.

### 4-4. 에이전트 직접 호출 (6종 각각)
```
> @speciai-advisory:legal 경업금지 조항 유효 요건 정리해줘
> @speciai-advisory:labor 5인 미만 사업장 연차 의무 있어?
> @speciai-advisory:tax 간이과세자 부가세 신고 기준 금액이랑 기한
> @speciai-advisory:finance 월 매출 3천만원, 고정비 1,800만원, 변동비율 30%일 때 손익분기랑 burn rate
> @speciai-advisory:bizplan 예비창업패키지 문제인식(P) 섹션 평가위원이 보는 포인트
> @speciai-advisory:verifier 이 문장의 인용 검증해줘: "대법원 2019다12345는 …"
```
합격: 각 에이전트가 시작 직후 `speciai_brief(<domain>)`을 호출하고, 결론에 산정식(노무·세무·재무)·조문·기한이 들어간다. verifier는 PASS/FAIL 판정표.

### 4-5. 팀 설정 연동
콘솔 "내 팀"에서 세무 에이전트를 끄고 맞춤 지시로 "항상 5인 미만 사업장 기준으로 답할 것"을 노무에 저장한 뒤:
```
> 부가세 신고 언제야?
> 연차 며칠 줘야 해?
```
합격: 첫 질문에 tax가 스폰되지 않고 "세무 에이전트가 꺼져 있음" 안내. 두 번째는 5인 미만 전제가 답에 반영.

### 4-6. 개인정보 마스킹
```
> 김철수(010-1234-5678, 900101-1234567) 직원이 무단결근 3일째인데 해고 가능해?
```
합격: 에이전트 프롬프트·검색 질의에 실명·주민번호가 그대로 들어가지 않고(마스킹), 답변은 "해당 직원"으로 지칭.

---

## 5. 계약서 (contract) ★

### 5-1. 프리랜서 용역
```
> /speciai-advisory:contract
> 프리랜서 앱 개발 용역. 갑 주식회사 테스트(대표 김대표), 을 홍길동. 대금 500만원 검수 후 7일 내 지급, 기간 2026-09-01~10-31, 특약: 소스코드 갑 귀속
```
기대 체인: `contract` → `speciai_brief(skill_contract)` → `speciai_doc_guide(docType:"프리랜서 용역계약서")` → `speciai_team` → 초안 → `verifier` → `speciai_verify_citation` → `build-docx.mjs --open`
합격:
- 질문은 한 번(필수 5개)만, 되묻기 없음
- 검수 기준·하자담보·IP 귀속(2차적저작물 포함)·책임 한도 조항이 각각 독립 조로 존재 (doc_guide required 반영)
- 위장 프리랜서 7대 요소 점검 언급
- DOCX 자동 열림, A4, 제N조(제목) 체계, 말미 면책 1줄
- 콘솔 문서함에 항목 생성, 클릭 시 A4 미리보기

### 5-2. 근로계약 — 법정 검증 게이트
```
> 정규직 근로계약서. 월급 200만원, 주 40시간, 5인 이상, 수습 3개월 90%
```
합격: 월 2,000,000 ÷ 209 = 9,569원 < 최저임금 10,320원 → 생성 중단·경고. 월급을 220만원으로 고치면 진행. 수습 감액은 1년 이상 계약일 때만 허용됨을 안내.

### 5-3. 자사 양식 첨부
```
> 우리 회사 표준 용역계약서 양식(첨부 .hwp)대로 조항 순서 맞춰서 써줘
```
기대 체인: `extract-doc.mjs 파일.hwp` 실행 → 조항 구성 추출 → 그 순서로 초안
합격: 양식의 조 제목·순서가 유지되고, 표준 골격은 빠진 필수 조항 보완에만 사용.

### 5-4. HWPX 동시 출력
```
> 한글 파일로도 줘
```
합격: `.hwpx` 생성, 한글 2010+에서 열림.

### 5-5. 무료 플랜 과금 게이트
무료 계정으로 5-1 실행 시: 주요 조항 3개 미리보기까지 + `speciai_paylink(doc_contract)` 링크·QR. 402 안내문이 그대로 표시.

---

## 6. 법률문서 (legaldoc) ★

### 6-1. 소장
```
> 거래처가 물품대금 3,300만원을 3개월째 안 줘. 소장 써줘. 우리는 개인사업자, 상대는 주식회사 ABC
```
기대 체인: `legaldoc` → `speciai_brief(skill_legaldoc)` → `speciai_doc_guide(docType:"물품대금 청구 소장")` (family=litigation, 법률구조공단 작성례 참조) → 초안 → `verifier`
합격:
- 표제 → 당사자(원고/피고) → 청구취지 → 청구원인(요건사실 순) → 입증방법 → 첨부서류 → 작성일 → 법원
- 관할법원·사건번호·상대방 주소는 공란 + "확인이 필요한 사항"
- 상인 여부 판단(상사 이율 6%·시효 5년) 명시, 인지액·관할 확인 항목

### 6-2. 보전처분
```
> 위 채권 가압류 신청서도 써줘. 상대 회사 거래은행 예금 대상
```
합격: 표제 "채권가압류신청", 제3채무자(은행) 당사자 추가, 청구채권의 표시·가압류할 채권의 표시 별지, 신청취지 정형문, 피보전권리·보전의 필요성 분리.

### 6-3. 답변서·준비서면
```
> 우리가 피고야. 물품에 하자가 있어서 대금 지급 거절했어. 답변서 써줘
```
합격: 청구취지에 대한 답변 → 청구원인에 대한 답변(인정/부인/항변 구분) → 동시이행항변·하자담보 근거.

### 6-4. 행정심판
```
> 구청에서 과태료 500만원 처분 받았는데 행정심판 청구서 써줘. 처분일 2026-08-01
```
합격: 청구기간(처분을 안 날 90일) 계산, 행정심판법 제30조 "중대한 손해" 문언 사용(행정소송법 문언 혼용 금지), 처분청·재결청 공란.

### 6-5. 강제집행
```
> 판결 받았는데 안 줘. 채권압류 및 추심명령 신청서
```
합격: 집행권원·집행문·송달증명 첨부 목록, 집행법원은 채무자 주소 기준이라 공란.

### 6-6. 회사 문서
```
> 주식회사 정관 만들어줘. 자본금 1억, 발행예정주식 100만주, 이사 3명 감사 1명
> 주주간계약서. 시드 투자자 지분 15%, 이사 1명 지명권, 우선주
> 취업규칙 초안. 10인 사업장, 주 40시간, 연차·징계 포함
```
합격: 정관은 상법 필수 기재사항(목적·상호·발행예정주식총수·1주 금액·본점·공고방법) 전부. 주주간계약은 사전동의 사항·희석방지 방식·드래그얼롱 요건이 숫자로 특정. 취업규칙은 10인 이상 신고 의무 안내.

### 6-7. 통지문·진술문서
```
> 가맹계약 해지 통고서
> 교통사고 경위 사실확인서
```
합격: 통고서에 계약서 조항 체계나 청구취지가 섞이지 않음(family=notice). 사실확인서는 시간순 사실만, 법리 서술 없음(family=statement).

### 6-8. speciai_doc_guide 직접 호출
```
> 가처분 신청서 필수 항목이 뭐야? 문서 가이드만 보여줘
```
합격: 문서군 판정 + 필수 항목 목록 + 주의 조항 + 근거 + 참조 작성례 제목·URL.

---

## 7. 내용증명 (certmail)

```
> /speciai-advisory:certmail
> 월세 보증금 2,000만원, 계약 2026-07-31 종료했는데 안 돌려줘. 임대인 박임대, 서울 강남구 ○○로 1
```
기대 체인: `certmail` → `speciai_brief(skill_certmail)` → 유형 분기(보증금 반환) → `speciai_doc_guide("보증금 반환 내용증명")` → 초안 → `speciai_verify_citation` → `build-docx.mjs`
합격: 발신·수신 블록, 1.사실관계 2.법적 근거(주택임대차보호법·민법 조문) 3.요구사항(기한 7~14일) 4.불이행 시 조치, 협박성 표현 없음, 3부 출력·등기 안내, 도달주의·최고 6개월 고지.

다른 유형 발동 문구: "임금 3개월 밀렸어 내용증명" → 임금체불 / "빌려준 돈 안 갚아" → 대여금 / "환불 안 해줘" → 청약철회.

---

## 8. 개인정보처리방침·이용약관 (privacy)

```
> /speciai-advisory:privacy
> B2B SaaS, 회원가입 이메일+구글 로그인, 결제 토스, AWS 서울 리전, GA 사용, 14세 미만 없음, AI 추천 기능 있음
```
기대 체인: `privacy` → `speciai_brief(skill_privacy)` → `speciai_doc_guide("개인정보처리방침")` → 인터뷰(한 번) → 초안 → `speciai_verify_citation`
합격: 제30조 필수 기재 14항목 체크리스트 통과, 자동화 결정(AI 추천) 조항 포함, 국외 이전(AWS) 조항, 표 형태 수집 항목·보유 기간, 이용약관은 별도 문서에 청약철회 7일.

---

## 9. 장부 (jangbu) ★

샘플 `bank.csv`(없으면 이걸로):
```csv
거래일자,적요,출금액,입금액,잔액
2026-07-02,스타벅스 강남점,12500,,987500
2026-07-03,GOOGLE ADS,330000,,657500
2026-07-05,입금 (주)클라이언트,,2200000,2857500
2026-07-10,배달의민족,89000,,2768500
2026-07-15,한국전력,45200,,2723300
2026-07-20,노무법인 미래,220000,,2503300
```
```
> 이 폴더의 bank.csv 장부로 정리해줘
```
기대 체인: `jangbu` → `speciai_brief(skill_jangbu)` → `speciai_team` → `jangbu.mjs seed` → `jangbu.mjs import bank.csv --type bank` → `jangbu.mjs classify` → 미분류 질문 → `jangbu.mjs set …` → `jangbu.mjs report`
합격:
- 시드 수신 "분류 룰 46개, 계정과목 47개"
- 자동 분류: 스타벅스→복리후생비, GOOGLE ADS→광고선전비, 배민 89,000(5만 초과)→접대비, 한전→전력비
- 미분류 2건(클라이언트 입금·노무법인)은 계정과목을 물어보고 확정 후 `--learn` 제안
- 손익 요약 + 계정별·월별, 접대비·불공제 언급
```
> 세무사한테 보낼 자료 뽑아줘
```
합격: `~/.speciai/jangbu/세무사전달_전체.csv`, 헤더에 국세청계정·국세청코드(복리후생비 811 등), 부가세 역산 추정치 주의 문구.

카드명세서 PDF 첨부:
```
> 이 카드명세서(첨부)도 장부에 넣어줘
```
합격: Read로 판독 → 표준 행 JSON → `jangbu.mjs add` → 할부는 회차 금액만, 취소 행은 inflow.

보안 확인: 이 절 전체에서 서버로 나가는 요청은 시드 1회뿐(콘솔 네트워크 탭 또는 `~/.speciai/events.jsonl`에 거래 내역 없음).

---

## 10. 증빙 OCR (receipt)

영수증·세금계산서 사진 첨부 후:
```
> 이 영수증 정리해줘
> 세금계산서 읽어서 장부에 넣어줘
```
기대 체인: `receipt` → `speciai_brief(skill_receipt)` → Read(이미지) → 검산 → `speciai_biz_status`(공급자 번호) → 표 → (요청 시) `jangbu.mjs add`
합격: 문서 유형 판별 근거 1줄, 공급자·사업자번호·일자·품목·공급가액·세액·합계 표, `공급가액+세액=합계` 검산, 사업자번호 체크섬 + 국세청 실사업자 확인, 흐린 값은 null+confidence low, 주민번호·카드번호 마스킹, 간이영수증 3만원 초과 시 적격증빙 경고.

---

## 11. 사업계획서 (bizplan)

```
> /speciai-advisory:bizplan
> 초기창업패키지. 아이템: 소상공인용 AI 세무 비서. 팀 2명. 공고문 첨부(pdf)
```
기대 체인: `bizplan` → `speciai_brief(skill_bizplan)` → `bizplan` 에이전트 → (공고문 있으면 `extract-doc.mjs`로 목차·배점) → PSST 4파트 → `verifier` 게이트(PASS까지 반복) → `build-docx.mjs` + `build-hwpx.mjs`
합격: 인터뷰 한 번, 공고 양식 목차 준수, 수치마다 출처 표기, 검증 리포트 부록. 무료 플랜은 문제인식(P) 미리보기까지 + `speciai_paylink(doc_bizplan)`.

---

## 12. 형사 의견서 (criminal) — 변호사 에디션 게이트

```
> /speciai-advisory:criminal 사기 혐의 피의자 의견서
```
합격(변호사 에디션 아님): `speciai_brief(skill_criminal)`가 402 `pack_required` → "변호사 에디션 전용" + `agent_lawyer` 결제 링크, 일반 구조(수사/공판 분기) 안내만. 파이프라인 미진행.
합격(변호사 에디션): 사실관계 정제 → 쟁점·증거 → 리서치 → 초안 → 검증 → A4.

---

## 13. 결제 (paylink)

```
> 사장님 올인원 결제 링크 줘
> 계약서 1건만 결제할래
```
합격: `speciai_paylink(agent_ceo)` → 링크 + 터미널 QR(25줄). 링크 열면 "사장님 올인원 99,000원" 모달·카드 결제 위젯. 건단위는 `doc_contract` 29,000원.
실결제 시: 30초 내 `/speciai-advisory:status` 활성 팩 5개 — 재시작 불필요. 402가 뜨던 기능이 바로 열린다.

---

## 14. 로컬 스크립트 직접 실행

```
$ cd <설치경로>/scripts
$ printf '# 테스트\n\n## 제1조(목적)\n\n본문.\n\n- 항목\n\n1. 번호\n' > t.md
$ node build-docx.mjs t.md t.docx --title "테스트" --open      # DOCX 열림, 번호 목록이 불릿으로 안 나옴
$ node build-hwpx.mjs t.md t.hwpx --title "테스트" --open      # 한글에서 열림
$ node extract-doc.mjs t.docx                                  # 제목 계층·본문 텍스트 출력
$ node extract-doc.mjs 기존계약서.hwp                           # HWP 5.0 본문 추출
$ node jangbu.mjs seed && node jangbu.mjs import bank.csv --type bank && node jangbu.mjs classify && node jangbu.mjs report --open
$ node dashboard.mjs                                            # 1초 내 "→ http://localhost:4747"
```
합격: 전부 에러 없이 1초 내 완료(성능 §17).

---

## 15. 콘솔 8페이지

http://localhost:4747 플로팅 네비 순서대로:

| 페이지 | 확인 | 합격 기준 |
|---|---|---|
| 내 팀 | 카드 클릭 켜기/끄기, 설정 모달에 맞춤 지시 저장 | `~/.speciai/agents.json` 갱신, CLI `speciai_team`에 즉시 반영 |
| 컴플라이언스 | 대시보드 로드 | 온보딩 업종 태그·트래킹 키워드·리스크 요약(계정 데이터) |
| 스킬 카탈로그 | 목록·검색 | 웹과 동일 |
| 채팅 | 에이전트 선택 → 메시지 | 웹 계정의 기존 대화 사이드바, 실시간 스트리밍, 새 대화가 웹에도 보임 |
| 워크플로우 | 노드 2~3개 캔버스에 배치·저장 | `~/.speciai/workflows.json`, CLI에서 "OO 워크플로우 실행해줘" 하면 그 순서대로 |
| 문서 편집기 | 진입 | 한글 편집기 메뉴바·툴바·캔버스(WASM), HWP 열기/저장/PDF |
| 문서함 | CLI에서 만든 문서 | 목록·A4 미리보기·열기 |
| 설정 | 키·서버 주소 | 마스킹 키 표시, 저장 시 `config.json` |

브라우저 콘솔(F12)에 빨간 에러 0건이어야 한다(열린국회 위젯 404 1건은 알려진 예외).

---

## 16. 보안·한도 게이트

| 테스트 | 방법 | 합격 |
|---|---|---|
| 분당 60회 리미터 | 스크립트로 status 70회 동시 호출 | 60개 이후 429 |
| 일일 한도 | 무료 계정으로 RAG 검색 11회 | 11회째 402 `daily_limit_reached` + 결제 링크·QR |
| 키 폐기 즉시 반영 | 웹 설정에서 키 revoke → CLI 호출 | 즉시 401 |
| 코드 유출 | 설치 폴더 전체 grep | 자문 지침·분류 룰·문서 규칙 텍스트 없음(SKILL.md는 트리거+brief 호출뿐) |
| 워터마크 | 두 계정으로 같은 brief 수신해 diff | 동의어 슬롯이 계정별로 다름 |
| 수집 탐지 | 한 계정이 하루 6개 이상 brief 도메인 조회 | 서버 로그 `수집 의심` 경고(운영자 확인) |
| 마스킹 왕복 | 채팅에 주민번호 포함 | 서버 로그·응답에 원문 미노출 |

---

## 17. 성능 기준치

| 항목 | 기준 | 실측(2026-08-26) |
|---|---|---|
| status / brief / doc_guide | 첫 0.5초, 이후 0.1초 | 0.31 / 0.09 / 0.15초 |
| 인용 검증 | 건당 0.6초(법제처) | 0.54초 |
| 국세청 조회 | 0.1초 | 0.008초 |
| DOCX / HWPX 생성(60개조) | 0.1초 | 0.04 / 0.03초 |
| 장부 5,000건 분류 | 0.1초 | 0.06초 |
| MCP 기동 | 0.1초, RSS 70MB 이하 | 0.03초, 64MB |
| 콘솔 기동 | 1초 | 0.14초 |
| 동시 30요청 | 전부 200, p95 1.5초 이하 | p95 1.16초 |

---

## 18. Windows 특이사항

- 스크립트 4종 모두 `process.platform` 분기(`cmd /c start`)·`path.join` 사용 — 셸 의존 없음.
- 환경변수는 `setx` 후 새 터미널부터. 콘솔 로그인(2-A)이면 `%USERPROFILE%\.speciai\config.json`에 저장돼 env 불필요.
- 한글 파일명 생성·열기 정상, CSV는 BOM 포함이라 Excel 한글 깨짐 없음.
- 콘솔 HWP 편집기는 브라우저 WASM — OS 무관. `.hwpx`는 한글 2010+.
- Windows에서만 확인할 것: §1 설치, §2-A 콘솔 로그인, §5-1 DOCX 자동 열림, §9 EUC-KR 은행 CSV(국내 은행 다운로드 원본) 가져오기, §14 `--open`.

---

## 19. 트러블슈팅

| 증상 | 조치 |
|---|---|
| 도구가 안 보임 | `claude plugin list` → Claude Code 재시작 → `/mcp` |
| "인증 실패: SPECIAI_API_KEY" | §2-A 또는 env 설정 후 재시작 |
| 스킬이 자동 발동 안 함 | 슬래시로 명시 호출해 비교. 자연어 트리거는 description 문구에 의존 |
| 국세청 조회 실패 | 10자리 확인, 콘솔 재실행 |
| 확인 메일 안 옴 | 스팸함, 시간당 30통 한도 — 구글 로그인 대체 |
| 문서 안 열림 | `build-docx.mjs` 직접 실행해 에러 확인 |
| 402 daily_limit | KST 자정 리셋 또는 결제 |
| 429 | 분당 60회 초과 — 정상 |
| 콘솔 흰 화면 | F12 콘솔 첫 에러 확인, `dashboard.mjs` 재실행 후 강력 새로고침 |

---

## 20. 결과 보고 양식

```
환경: macOS 15 / Windows 11, Node 22.x, Claude Code x.y, 플랜: 무료/agent_ceo/agent_lawyer
[1 설치] PASS/FAIL
[2 키] PASS/FAIL — 방식 A/B, 이메일 확인메일 도착 O/X
[3 기본 도구] status / biz_status / verify / law_search / search / team — 각 PASS/FAIL
[4 자문] 단일 / 병렬·상충 / 에이전트 6종 / 팀 연동 / 마스킹 — 각 PASS/FAIL
[5 계약서] 용역 / 근로 최저임금 게이트 / 양식 첨부 / HWPX / 과금 게이트
[6 법률문서] 소장 / 가압류 / 답변서 / 행정심판 / 강제집행 / 정관·SHA·취업규칙 / 통고서·확인서
[7 내용증명] [8 개인정보] [9 장부] [10 증빙] [11 사업계획서] [12 형사(게이트)] [13 결제]
[14 스크립트] [15 콘솔 8페이지] [16 보안 게이트] [17 성능]
특이사항: 증상 · 재현 프롬프트 · 발동 체인 캡쳐 · 스크린샷
```
